"use client";
// Phase 0 stub — real rAF parallax driver lands in Phase 1 (Task 1.2).
// Keys off the page-local data-mx-parallax attribute only (not the shared
// parallax attribute), so the global MillerParallax stays inert here.
export function MxParallax() {
  return null;
}
