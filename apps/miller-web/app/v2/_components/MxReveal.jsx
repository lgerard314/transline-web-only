"use client";
// Phase 0 stub — real 4-verb reveal driver lands in Phase 1 (Task 1.1).
// Keys off the page-local data-mx-reveal attribute only (not the shared
// scroll-reveal attribute), so the global MillerScrollReveal stays inert here.
export function MxReveal() {
  return null;
}
